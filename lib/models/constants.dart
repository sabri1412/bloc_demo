import 'package:flutter/material.dart';

const dbDuration = Duration(milliseconds: 700);
const alertText = TextStyle(fontSize: 16, color: Colors.white);
const alertTitle =
    TextStyle(fontSize: 20, color: Colors.white, fontWeight: FontWeight.bold);
const titleText = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);

var backgroundColor = Colors.black.withOpacity(0.85);
